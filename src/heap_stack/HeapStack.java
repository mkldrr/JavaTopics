package heap_stack;

import final_finalize_finally.Final;

import java.util.Stack;

public class HeapStack {
//    There are two places that java uses from RAM memory
//—Stack:
//            —primitive datetypes  : int age 23;
//     — references = Bag bag1 = new Bag( ): bag1 is the reference.
//            — method calls : ex. MyCollection
//—Heap:
//            —actual Objects are stored in heap.
//    Recap:
//    This:                referencing variable
//    This ( ) :          when we call constructor
//    Static :            belongs directly to class
//    Static block:   execute first
//    Static import: when we do static import, we don’t need to mention name of the class
//    Importing static members of the class in the static way, so that we can use them without
//    Mentioning the class name.
//    Heap :             will hold objects , when there is new keyword, they go to heap
//    Stack:              holds primitive datatypes
//    String pool:    strings are going here
//    Immutable:     objects that cannot be changed
//    Mutable:          objects cannot be changeable
//    StringBuilder: allow us to make objects  mutable
//    Final keyword: it can be used for class, method and variables.
}
