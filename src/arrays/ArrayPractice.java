package arrays;

import java.util.Arrays;
import java.util.Collections;

public class ArrayPractice {

//    Collections: one group that can hold multiple things of the same kind.
//—-Storing different variables into one array.
//            —- arrays are the first type of collection we will use
//—- we use collections to collect info of the same kind
//    dataType [ ] variableName = new dataType[ size] ;

//        —We learned Arrays.toString ( )
//            —Sorting an Array
//> Arranging the element in order from smallest to largest
//—Searching in an Array
//> Java allows us to search for an element in an array, but only if the array is sorted.
//            Arrays.binarySearch(arrayName), element)

//    Possible result;
//-If found, will give index of the element
//if not found, will return negative number. (index + 1)
//    If array is not sorted, the result is not predictable.
//— String Manipulation Methods in arrays
//> split ( )

//  > used on a string and converts the string to an string array based on the given characters
//  — takes: string
//  — return: string

//>toCharArray ( )
// > takes a string and returns it into a character array
// — takes nothing
// — return char


    public static void main(String[] args) {








    }


}
